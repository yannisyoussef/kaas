# Observability

The target baseline is OpenTelemetry traces and metrics from API, broker consumers, worker, and sandbox lifecycle, plus structured JSON logs. Exporters are environment-configured; local Grafana/collector deployment is intentionally deferred until the first runnable slice defines real signals and dashboards.
